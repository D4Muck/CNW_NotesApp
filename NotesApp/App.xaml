<Application
    x:Class="NotesApp.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NotesApp.Model"
    xmlns:service="using:NotesApp.Service"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    RequestedTheme="Light">

    <Application.Resources>
        <service:ViewModelLocator x:Key="Locator" />
        <DataTemplate x:Key="NoteDetailTemplate" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock TextAlignment="Right" Grid.Row="0" Text="{Binding Time}" />
                <TextBlock Grid.Row="1" Text="{Binding Text}" />
            </Grid>
        </DataTemplate>
    </Application.Resources>
</Application>