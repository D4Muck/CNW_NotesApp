<Page
    x:Class="NotesApp.View.AddNotePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NotesApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.DataContext>
        <Binding Path="AddNoteViewModel" Source="{StaticResource Locator}" />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Right" Text="{x:Bind ViewModel.Note.Time, Mode=OneWay}" />
        <TextBox Grid.Row="1" Text="{Binding Note.Text,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap" AcceptsReturn="True" />
        <Button IsEnabled="{x:Bind ViewModel.CanAddNote,Mode=OneWay}" VerticalAlignment="Top"
                HorizontalAlignment="Stretch" Grid.Row="2" Content="Add"
                Click="{x:Bind ViewModel.AddNote}" />
    </Grid>
</Page>