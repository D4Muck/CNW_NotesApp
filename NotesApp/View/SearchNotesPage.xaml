<Page xmlns:my="using:NotesApp.View"
      x:Class="NotesApp.View.SearchNotesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:model="using:NotesApp.Model"
      xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
      mc:Ignorable="d"
      DataContext="{Binding SearchNotesViewModel, Source={StaticResource Locator}}">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <Page.Resources>
        <DataTemplate x:Key="MasterListViewItemTemplate" x:DataType="model:Note">
            <StackPanel>
                <TextBlock Text="{Binding Time}" />
                <TextBlock Text="{Binding Text}" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="DetailContentTemplate" x:DataType="model:Note">
            <StackPanel>
                <TextBlock Text="{Binding Time}" />
                <TextBlock Text="{Binding Text}" />
            </StackPanel>
        </DataTemplate>

        <my:NullToBoolConverter x:Key="NullToBoolConverter" />
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" x:Name="LayoutRoot"
          Loaded="LayoutRoot_Loaded">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="AdaptiveStates" CurrentStateChanged="AdaptiveStates_CurrentStateChanged">
                <VisualState x:Name="DefaultState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                </VisualState>

                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="MasterColumn.Width" Value="*" />
                        <Setter Target="DetailColumn.Width" Value="0" />
                        <Setter Target="MasterListView.SelectionMode" Value="None" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="MasterColumn" Width="320" />
            <ColumnDefinition x:Name="DetailColumn" Width="*" />
        </Grid.ColumnDefinitions>

        <TextBox Grid.Row="0" Text="{Binding SearchText, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                 KeyDown="{x:Bind ViewModel.UpdateNotes}" />
        <ListView
            x:Name="MasterListView"
            Grid.Row="1"
            ItemContainerTransitions="{x:Null}"
            ItemTemplate="{StaticResource MasterListViewItemTemplate}"
            IsItemClickEnabled="True"
            ItemsSource="{x:Bind ViewModel.Notes}"
            ItemClick="MasterListView_ItemClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Grid Grid.Column="1"
              Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <CommandBar Grid.Row="0">
                <AppBarButton Icon="Edit"
                              IsEnabled="{Binding SelectedItem,ElementName=MasterListView,Mode=OneWay,Converter={StaticResource NullToBoolConverter}}"
                              Label="Edit Note" Click="ButtonEdit_OnClick" />

                <CommandBar.Content>
                    <TextBlock Text="Note Detail" Margin="12,14" />
                </CommandBar.Content>
            </CommandBar>
            <ContentPresenter Grid.Row="1" ContentTemplate="{StaticResource NoteDetailTemplate}"
                              Content="{Binding SelectedItem,ElementName=MasterListView , Mode=OneWay}"
                              x:Name="DetailContentPresenter" />
            <maps:MapControl Grid.Row="2" x:Name="map"
                             ZoomLevel="5" />


        </Grid>
    </Grid>
</Page>