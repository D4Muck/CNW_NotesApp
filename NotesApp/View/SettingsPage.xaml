<Page
    x:Class="NotesApp.View.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NotesApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.DataContext>
        <Binding Path="SettingsViewModel" Source="{StaticResource Locator}" />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock>Number of notes shown</TextBlock>
            <TextBox InputScope="Number" Text="{Binding NotesShown,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock>Check for Ascending, else Descending</TextBlock>
            <CheckBox IsChecked="{Binding Ascending,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Grid>
</Page>